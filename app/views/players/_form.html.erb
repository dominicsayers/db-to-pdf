<%= form_for(player) do |f| %>
  <% if player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %>
    <%= f.text_field :full_name %>
  </div>

  <div class="field">
    <%= f.label :date_of_birth %>
    <%= f.datetime_select :date_of_birth %>
  </div>

  <div class="field">
    <%= f.label :allergic %>
    <%= f.check_box :allergic %>
  </div>

  <div class="field">
    <%= f.label :medication %>
    <%= f.check_box :medication %>
  </div>

  <div class="field">
    <%= f.label :photo_gumshield %>
    <%= f.text_field :photo_gumshield %>
  </div>

  <div class="field">
    <%= f.label :photo_no_gumshield %>
    <%= f.text_field :photo_no_gumshield %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
